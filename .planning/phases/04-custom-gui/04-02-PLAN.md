---
phase: 04-custom-gui
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - GUI-01
  - GUI-02
  - GUI-03
  - GUI-04
  - GUI-05

must_haves:
  truths:
    - "Plugin opens custom editor window with dark navy background and teal accents"
    - "Five sections are visible: Pitch, Harmonics, Scan/Tilt, FM, Output"
    - "Eight harmonic faders respond to mouse drag and show teal fill"
    - "All knobs respond to vertical drag and display current values"
    - "Output ComboBox dropdown shows four modes and switches selection"
    - "Window resizes smoothly with proportional layout"
  artifacts: []
  key_links: []
---

<objective>
Visually verify the custom GUI in a DAW or standalone host.

Purpose: GUI work is inherently visual — automated build checks cannot confirm layout, colors, proportions, or usability. A human must verify the editor looks and works as designed.
Output: Confirmation that the GUI meets the design specification, or a list of issues to fix.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-custom-gui/04-CONTEXT.md
@.planning/phases/04-custom-gui/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of custom GUI</name>
  <files>n/a</files>
  <action>
Human visual verification of the custom plugin editor built in Plan 04-01.

What was built: Custom plugin editor with 5-section two-row layout, dark flat aesthetic, 8 harmonic faders, rotary knobs, output selector ComboBox, and resizable window.

Steps to verify:
1. Open the plugin in a DAW (REAPER, Ableton, Logic) or launch the Standalone build from `build/HarmonicOscillator_artefacts/Release/Standalone/Harmonic Oscillator.app`
2. Verify the editor window opens at ~700x450 with dark navy background (#1a1a2e)
3. Confirm five sections are visible with subtle panel cards (#222244): Pitch (top-left), Harmonics (top-right), Scan/Tilt (bottom-left), FM (bottom-center), Output (bottom-right)
4. Check section headers are centered and labeled correctly
5. Drag harmonic faders — verify teal fill (#00d4aa) rises from bottom, value text updates below each fader
6. Drag rotary knobs — verify teal arc, white indicator dot, value text below
7. Double-click a knob or fader — verify it resets to its default value
8. Click the output selector ComboBox — verify dropdown shows "Harmonic Mix", "Triangle", "Sawtooth", "Square"
9. Resize the window by dragging the corner — verify it scales from ~525x338 to ~1050x675 with layout staying proportional
10. Play a MIDI note — verify audio still works (DSP unchanged)

Resume signal: Type "approved" if the GUI looks and works correctly, or describe any issues (layout problems, color mismatches, control issues, etc.)
  </action>
  <verify>
    <automated>echo "Human visual verification — no automated check"</automated>
  </verify>
  <done>User approves the GUI appearance and interaction, confirming it matches the design specification from 04-CONTEXT.md.</done>
</task>

</tasks>

<verification>
Human confirms visual appearance and interaction matches the design specification from 04-CONTEXT.md.
</verification>

<success_criteria>
- User approves the GUI appearance and interaction
- Or: user provides specific feedback that can be addressed in a follow-up gap closure plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-custom-gui/04-02-SUMMARY.md`
</output>
