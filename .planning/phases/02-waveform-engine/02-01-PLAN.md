---
phase: 02-waveform-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dsp/HarmonicEngine.h
  - src/dsp/HarmonicEngine.cpp
autonomous: true
requirements: [DSP-01, DSP-02, DSP-03, DSP-04, DSP-05, DSP-06, WAVE-01, WAVE-02, WAVE-03, WAVE-04, PERF-07]

must_haves:
  truths:
    - "HarmonicEngine class compiles with all DSP methods implemented"
    - "8 independent per-harmonic phase accumulators produce sine output"
    - "PolyBLEP saw/square/triangle waveforms are computed per-sample"
    - "Scan Gaussian window reshapes harmonic amplitudes"
    - "Spectral tilt applies linear-in-dB slope across harmonics"
    - "DC blocker filters harmonic mix at 5Hz"
    - "Output selector hard-switches between 4 waveform modes"
  artifacts:
    - path: "src/dsp/HarmonicEngine.h"
      provides: "Complete class declaration with all DSP state and public API"
      exports: ["HarmonicEngine"]
    - path: "src/dsp/HarmonicEngine.cpp"
      provides: "Full DSP implementation: renderSample, prepare, handleNoteOn/Off"
      min_lines: 150
  key_links:
    - from: "src/dsp/HarmonicEngine.h"
      to: "APVTS parameter IDs"
      via: "std::atomic<float>* cached pointers"
      pattern: "getRawParameterValue"
    - from: "src/dsp/HarmonicEngine.cpp"
      to: "gen~ codebox algorithms"
      via: "line-by-line translation"
      pattern: "harmPhases_|triIntegrator_|dcR_|scanEnv_|tiltFactors_"
---

<objective>
Create the HarmonicEngine DSP class that implements the complete gen~ codebox as C++.

Purpose: This is the core sound engine -- all waveform generation, harmonic processing, scan/tilt shaping, DC blocking, and output selection happens here. The gen~ codebox is translated line-by-line into a single coordinator class.

Output: `src/dsp/HarmonicEngine.h` and `src/dsp/HarmonicEngine.cpp` -- a self-contained DSP engine ready to be wired into PluginProcessor in Plan 02.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-waveform-engine/02-RESEARCH.md

@src/PluginProcessor.h
@src/PluginProcessor.cpp
</context>

<interfaces>
<!-- APVTS parameter IDs from PluginProcessor.cpp that HarmonicEngine must reference -->

Parameter IDs (all version 1):
- harm_1 through harm_8 (float 0-1, default: harm_1=1.0, rest=0.0)
- scan_center (float 0-1, default 0.5)
- scan_width (float 0-1, default 1.0)
- spectral_tilt (float -1 to 1, default 0.0)
- master_level (float 0-1, default 0.8)
- output_select (choice: 0=Harmonic Mix, 1=Triangle, 2=Sawtooth, 3=Square)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create HarmonicEngine header with full interface</name>
  <files>src/dsp/HarmonicEngine.h</files>
  <action>
Create directory `src/dsp/` and file `src/dsp/HarmonicEngine.h`.

The header defines the complete HarmonicEngine class:

**Public API:**
- `void prepare(double sampleRate, int maxBlockSize, juce::AudioProcessorValueTreeState& apvts)` — caches sample rate, computes DC blocker coefficient, resets all state, caches APVTS parameter pointers
- `double renderSample()` — produces one output sample (the main DSP method)
- `void handleNoteOn(int midiNote, float velocity)` — sets frequency, velocity, opens gate
- `void handleNoteOff(int midiNote)` — closes gate (simple, no gate counting yet)

**Private state (all `double` except where noted):**
- `double sampleRate_`, `double inverseSampleRate_` — cached from prepare
- `double phase_` — master phase accumulator for PolyBLEP waveforms
- `std::array<double, 8> harmPhases_` — per-harmonic phase accumulators (CRITICAL: independent, not shared)
- `double triIntegrator_` — leaky integrator state for triangle wave
- `double dcX1_`, `double dcY1_` — DC blocker state
- `double dcR_` — DC blocker coefficient (computed from 5Hz cutoff)
- `std::array<double, 8> scanEnv_` — scan Gaussian envelope per harmonic
- `std::array<double, 8> tiltFactors_` — tilt multiplier per harmonic
- `double currentFreq_` — current note frequency in Hz
- `double currentVelocity_` — current note velocity (0-1)
- `bool gateOpen_` — simple gate state
- `double gateLevel_` — smoothed gate for 5ms anti-click ramp
- `double gateAttackCoeff_`, `double gateReleaseCoeff_` — computed from 5ms / sample rate
- `juce::SmoothedValue<float> masterLevelSmoothed_` — smoothed master level
- `std::array<juce::SmoothedValue<float>, 8> harmLevelSmoothed_` — smoothed harmonic faders

**Cached APVTS pointers (private):**
- `std::array<std::atomic<float>*, 8> harmLevelParams_`
- `std::atomic<float>* scanCenterParam_`
- `std::atomic<float>* scanWidthParam_`
- `std::atomic<float>* spectralTiltParam_`
- `std::atomic<float>* masterLevelParam_`
- `std::atomic<float>* outputSelectParam_`

Include `<juce_audio_processors/juce_audio_processors.h>`, `<array>`, `<cmath>`, `<atomic>`.

Use `#pragma once`. Wrap in no namespace (flat, like PluginProcessor).
  </action>
  <verify>
    <automated>cd /Users/matt/src/harmonic-oscillator && grep -c "harmPhases_\|renderSample\|handleNoteOn\|dcR_\|scanEnv_\|tiltFactors_\|gateLevel_" src/dsp/HarmonicEngine.h</automated>
  </verify>
  <done>HarmonicEngine.h exists with all member variables and method declarations matching gen~ codebox state requirements</done>
</task>

<task type="auto">
  <name>Task 2: Implement HarmonicEngine DSP (gen~ codebox translation)</name>
  <files>src/dsp/HarmonicEngine.cpp</files>
  <action>
Create `src/dsp/HarmonicEngine.cpp` implementing all methods. This is a line-by-line translation of the gen~ codebox sections 2-9. Follow the research document code examples EXACTLY.

**`prepare()` implementation:**
- Store sampleRate, compute inverseSampleRate = 1.0 / sampleRate
- Compute DC blocker coefficient: `dcR_ = 1.0 - (twoPi * 5.0 * inverseSampleRate_)`
- Compute gate ramp coefficients for 5ms anti-click: `gateAttackCoeff_ = 1.0 / (0.005 * sampleRate)` and `gateReleaseCoeff_ = 1.0 / (0.005 * sampleRate)` (both 5ms)
- Reset ALL state to zero: phase_, harmPhases_.fill(0.0), triIntegrator_, dcX1_, dcY1_, scanEnv_.fill(1.0), tiltFactors_.fill(1.0), gateOpen_ = false, gateLevel_ = 0.0, currentFreq_ = 440.0, currentVelocity_ = 0.0
- Initialize SmoothedValue for masterLevelSmoothed_ (reset to 0.8, ramp length 64 samples) and each harmLevelSmoothed_ (reset to default, ramp length 64)
- Cache all APVTS parameter pointers using `apvts.getRawParameterValue("param_id")` with exact IDs from PluginProcessor: harm_1..harm_8, scan_center, scan_width, spectral_tilt, master_level, output_select

**`renderSample()` implementation — translate gen~ codebox sections 2-9:**

Section 2 — Master phase accumulator:
```
double phaseInc = currentFreq_ * inverseSampleRate_;
double p = phase_ + phaseInc;
p -= std::floor(p);
phase_ = p;
```

Section 3 — PolyBLEP sawtooth:
```
double sawRaw = 2.0 * p - 1.0;
double dt = phaseInc;
// PolyBLEP correction at discontinuity near 0/1 boundary
double sawBlep = 0.0;
if (p < dt) { double t = p / dt; sawBlep = t + t - t * t - 1.0; }
if (p > 1.0 - dt) { double t = (p - 1.0) / dt; sawBlep = t * t + t + t + 1.0; }
double sawOut = sawRaw - sawBlep;
```

Section 3 — PolyBLEP square:
```
double sqrRaw = (p < 0.5) ? 1.0 : -1.0;
double sqrBlep = 0.0;
// Correction at 0/1 boundary
if (p < dt) { double t = p / dt; sqrBlep += t + t - t * t - 1.0; }
else if (p > 1.0 - dt) { double t = (p - 1.0) / dt; sqrBlep += t * t + t + t + 1.0; }
// Correction at 0.5 boundary
if (p >= 0.5 && p < 0.5 + dt) { double t = (p - 0.5) / dt; sqrBlep -= t + t - t * t - 1.0; }
else if (p > 0.5 - dt && p < 0.5) { double t = (p - 0.5) / dt; sqrBlep -= t * t + t + t + 1.0; }
double sqrOut = sqrRaw + sqrBlep;
```

Section 3 — Triangle (leaky integrated square, fixed leak 0.999):
```
double triRaw = triIntegrator_ * 0.999 + sqrOut * phaseInc * 4.0;
triIntegrator_ = triRaw;
double triOut = std::clamp(triRaw, -1.0, 1.0);
```

Section 4 — Scan Gaussian envelope:
```
double centerPos = static_cast<double>(*scanCenterParam_) * 7.0;
double sigma = 0.3 + static_cast<double>(*scanWidthParam_) * 9.7;
for (int i = 0; i < 8; ++i) {
    double dist = (static_cast<double>(i) - centerPos) / sigma;
    scanEnv_[i] = std::exp(-0.5 * dist * dist);
}
```

Section 5 — Spectral tilt:
```
double tiltParam = static_cast<double>(*spectralTiltParam_);
for (int i = 0; i < 8; ++i) {
    double normPos = static_cast<double>(i) / 7.0;
    double tiltDb = tiltParam * (normPos - 0.5) * 24.0;
    tiltFactors_[i] = std::pow(10.0, tiltDb / 20.0);
}
```

Section 6 — 8-harmonic sine bank with per-harmonic phase accumulators:
```
double harmonicMix = 0.0;
double nyquist = sampleRate_ * 0.5;
for (int i = 0; i < 8; ++i) {
    // Update SmoothedValue target from APVTS
    harmLevelSmoothed_[i].setTargetValue(*harmLevelParams_[i]);
    double fader = static_cast<double>(harmLevelSmoothed_[i].getNextValue());

    int harmNum = i + 1;
    double harmFreq = currentFreq_ * harmNum;
    if (harmFreq < nyquist) {
        double hPhase = harmPhases_[i] + harmFreq * inverseSampleRate_;
        hPhase -= std::floor(hPhase);
        harmPhases_[i] = hPhase;
        double amp = fader * scanEnv_[i] * tiltFactors_[i];
        harmonicMix += std::sin(hPhase * juce::MathConstants<double>::twoPi) * amp;
    } else {
        harmPhases_[i] = 0.0;
    }
}
harmonicMix *= 0.25;  // Normalization factor from gen~
```

Section 8 — DC blocker (5Hz highpass):
```
double mixDC = harmonicMix - dcX1_ + dcR_ * dcY1_;
dcX1_ = harmonicMix;
dcY1_ = mixDC;
```

Section 9 — Gate, master level, output selector:
```
// 5ms anti-click gate ramp
double gateTarget = gateOpen_ ? 1.0 : 0.0;
if (gateLevel_ < gateTarget)
    gateLevel_ = std::min(gateLevel_ + gateAttackCoeff_, gateTarget);
else if (gateLevel_ > gateTarget)
    gateLevel_ = std::max(gateLevel_ - gateReleaseCoeff_, gateTarget);

double env = gateLevel_ * currentVelocity_;

// Smoothed master level
masterLevelSmoothed_.setTargetValue(*masterLevelParam_);
double masterLvl = static_cast<double>(masterLevelSmoothed_.getNextValue());

int outputMode = static_cast<int>(*outputSelectParam_);
double selected;
switch (outputMode) {
    case 0: selected = mixDC; break;
    case 1: selected = triOut; break;
    case 2: selected = sawOut; break;
    case 3: selected = sqrOut; break;
    default: selected = mixDC; break;
}
return selected * env * masterLvl;
```

**`handleNoteOn()` implementation:**
- Set `currentFreq_ = juce::MidiMessage::getMidiNoteInHertz(midiNote)`
- Set `currentVelocity_ = velocity`
- Set `gateOpen_ = true`

**`handleNoteOff()` implementation:**
- Set `gateOpen_ = false` (simple, any note-off closes; proper gate counting deferred to Phase 3)

IMPORTANT: Use `double` for ALL internal DSP state. Only cast to `float` at the final output in processBlock (done in Plan 02). Use `juce::MathConstants<double>::twoPi` for the two-pi constant.
  </action>
  <verify>
    <automated>cd /Users/matt/src/harmonic-oscillator && grep -c "renderSample\|prepare\|handleNoteOn\|handleNoteOff\|std::sin\|std::exp\|std::pow\|std::clamp\|dcR_\|harmPhases_\|triIntegrator_\|polyBLEP\|scanEnv_\|tiltFactors_\|0\.25\|0\.999\|SmoothedValue\|getNextValue" src/dsp/HarmonicEngine.cpp</automated>
  </verify>
  <done>HarmonicEngine.cpp compiles with all gen~ codebox sections translated: phase accumulators, PolyBLEP saw/square, leaky triangle, scan Gaussian, spectral tilt, DC blocker at 5Hz, output selector, 5ms anti-click gate, SmoothedValue for master level and harmonic faders, 0.25 normalization factor</done>
</task>

</tasks>

<verification>
- HarmonicEngine.h declares the full public API and all private state
- HarmonicEngine.cpp implements all gen~ codebox sections 2-9
- Per-harmonic phase accumulators (not shared phase multiplication)
- DC blocker at 5Hz (not 40Hz)
- Triangle leak fixed at 0.999 (not frequency-dependent)
- Scan sigma = 0.3 + width * 9.7
- Tilt: tiltDb = tilt * (normPos - 0.5) * 24.0
- Harmonic mix normalized by 0.25
- 5ms anti-click gate ramp (not hard gate, not full AR envelope)
- SmoothedValue on master_level and 8 harmonic faders
</verification>

<success_criteria>
Both files exist and contain the complete DSP implementation matching the gen~ codebox algorithms from the research document. All 8 harmonics use independent phase accumulators. The class is self-contained and ready to wire into PluginProcessor.
</success_criteria>

<output>
After completion, create `.planning/phases/02-waveform-engine/02-01-SUMMARY.md`
</output>
